import { paddingX } from "@/constants/layout";

export default function Footer() {
    return (
        <div id='footer' className={`fixed bottom-0 w-full bg-white border border-t flex justify-between ${paddingX} py-2`}>
            footer
        </div>
    );
}
